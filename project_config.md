# 技术配置
当前阶段：Phase 1
核心工具：Cursor + DirectShell MCP + Node.js

## Phase 1 状态
- MCP 配置骨架：已完成（`.cursor/mcp.json` + 说明见 `docs/CURSOR_MCP_配置说明.md`）。
- DirectShell 获取：等待 IamLumae/DirectShell 作者在 GitHub Releases 发布 Windows 预编译包（不采用源码编译）。
- 语音模块：接口设计已预留，见 `docs/voice_module_spec.md`。
